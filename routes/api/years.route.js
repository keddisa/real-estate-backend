// var express = require('express');
// var router = express.Router();
// const checkAuth = require ('../../middleware/check-auth');

// var YearController = require('../../controllers/year.controller.js');

// router.get('/', YearController.getYears);
// router.post('/', checkAuth, YearController.createYear);
// router.put('/', checkAuth, YearController.updateYear);
// router.delete('/:id', checkAuth, YearController.removeYear);
// router.get('/:id', YearController.getYear);

// module.exports = router;